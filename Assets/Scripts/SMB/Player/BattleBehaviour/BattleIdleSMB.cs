using UnityEngine;
using System.Collections;

public class BattleIdleSMB : StateMachineBehaviour {
  /*
  override public void OnStateEnter(Animator animator, AnimatorStateInfo stateInfo, int layerIndex) {
    base.OnStateEnter(animator, stateInfo, layerIndex);
    _currentStateName = "Idle";
    Debug.Log("Idle");
  }

  override public void OnStateUpdate(Animator animator, AnimatorStateInfo stateInfo, int layerIndex) {
    if (_photonView.isMine) {
      if ( ShouldTransitionTo( "Die"        ) ) { ActTransition( "Die"        , animator ); return; }
      if ( ShouldTransitionTo( "Skill"      ) ) { ActTransition( "Skill"      , animator ); return; }
      if ( ShouldTransitionTo( "Climb"      ) ) { ActTransition( "Climb"      , animator ); return; }
      if ( ShouldTransitionTo( "Walk"       ) ) { ActTransition( "Walk"       , animator ); return; }
      if ( ShouldTransitionTo( "LieDown"    ) ) { ActTransition( "LieDown"    , animator ); return; }
      if ( ShouldTransitionTo( "GroundJump" ) ) { ActTransition( "GroundJump" , animator ); return; }
      if ( ShouldTransitionTo( "Fall"       ) ) { ActTransition( "Fall"       , animator ); return; }
    }
  }
  */
}

