using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
public class BattlePlayerSMB : LobbyPlayerSMB {
  override public void OnStateEnter(Animator animator, AnimatorStateInfo stateInfo, int layerIndex) {
    base.OnStateEnter(animator, stateInfo, layerIndex);

    if (_photonView == null)
      _hp         = animator.GetComponent<PlayerHp>();
  }

  protected override bool ShouldTransitionTo(string stateName) {
    if (stateName == "Die")
      return _hp.Dead;

    return base.ShouldTransitionTo(stateName);
  }

  private static PlayerHp _hp;
}
*/

